import {addGroup} from './group';
import {displayAllCards} from './cards';
import { addTerm, deleteTerm, editTerm, emptyTerm } from './term';

export { addTerm, deleteTerm, editTerm, emptyTerm , displayAllCards, addGroup };

//// first import all action component in a single file
//// then export all of this